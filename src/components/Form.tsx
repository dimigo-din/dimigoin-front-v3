import React from "react";
import styled from "@emotion/styled";

export const FormHeader = styled.h2`
  font-size: 20px;
  font-weight: 900;
  margin-bottom: 18px;
  &:not(:first-of-type) {
    margin-top: 36px;
  }
`;
